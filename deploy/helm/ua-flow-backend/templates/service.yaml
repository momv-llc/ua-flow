apiVersion: v1
kind: Service
metadata:
  name: {{ include "ua-flow-backend.fullname" . }}
  labels:
    {{- include "ua-flow-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ua-flow-backend.selectorLabels" . | nindent 4 }}
